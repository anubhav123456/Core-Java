
---

# 📘 **Notes: Compile Time String Constants**
---

## **📌 Types of Compile-Time String Constants in Java**

A string becomes a **compile-time constant** only if the compiler can determine its value *during compilation*, not at runtime. Such constants are stored directly in the **String Constant Pool (SCP)**.

---

### **### 1️⃣ String Literals**

These are direct string values defined in code.

```java
String s = "Hello";
```

✔ Stored in SCP
✔ Compile-time constant

---

### **2️⃣ Concatenation of Only Literal Strings**

If all operands are string *literals*, the concatenation is computed at compile time.

```java
String s = "Hello" + "World"; // → "HelloWorld"
```

✔ Evaluated at compile time
✔ Stored in SCP

---

### **3️⃣ `final` Strings Assigned Literal Values at Declaration**

A final variable becomes a compile-time constant **only when assigned a literal at the same line**.

```java
final String s1 = "Java";
String s2 = s1 + " Rocks"; // → Compile-time concatenation
```

✔ `s1` is a compile-time constant
✔ Result: `"Java Rocks"` goes to SCP

---

### **4️⃣ Multiple Final Literal Parts**

```java
final String a = "Core";
final String b = "Java";
String c = a + b; // → "CoreJava"
```

✔ Both operands are compile-time constants
✔ Result stored in SCP

---

### **⚠ Not Compile-Time Constants**

#### ❌ Final variable assigned later

```java
final String s;
s = "Hello"; // Assigned later
String result = s + "World"; // Runtime concatenation
```

✔ Result created on heap
❌ NOT compile-time constant

---

#### ❌ Involves new String()

```java
final String s = new String("Hello");
String str = s + "World"; // Runtime
```

Because `"new String()"` creates a runtime object → not constant.

---

#### ❌ Non-final variable

```java
String s = "Hello";
String result = s + "World"; // Runtime
```

---




